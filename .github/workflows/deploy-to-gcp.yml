name: Teste de Validação YAML

on:
        push:
                branches:
                        - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Apenas um passo de teste
      run: echo "Hello, GitHub Actions!"
